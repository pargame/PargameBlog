/**
 * DEPRECATED: 이 파일은 더 이상 사용되지 않습니다.
 * 원본 구현은 `src/hooks/useGraphSimulation.ts`에 있으며, 모든 호출자는
 * 해당 경로로 직접 import 하도록 코드가 이미 마이그레이션 되어 있습니다.
 *
 * 안전한 제거를 위해 파일을 빈화(deprecate)합니다. 실제 삭제는 PR에서
 * 수동으로 수행하세요.
 */

export {}; // 빈 export로 모듈을 유효하게 유지 (의도적인 비사용 상태 표시)
